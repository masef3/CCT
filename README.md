***CCT***

To simply start using the program just use `make`.

To add the binary to local path use `make install`.

To clean directory `make clean`.

To rebuild program `make rebuild`.

To run the program on a file:

```
./cct filename.folded
```


